{
  "name": "@morgan-wrestling/auth",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts",
    "./permissions": "./src/permissions.ts",
    "./lib/auth": "./src/lib/auth.ts",
    "./lib/auth-client": "./src/lib/auth-client.ts"
  },
  "scripts": {
    "auth:generate": "bun x auth generate --config ./src/config.ts --output ./src/schema.ts",
    "db:generate": "bun --bun drizzle-kit generate",
    "db:migrate": "bun --bun drizzle-kit migrate",
    "db:up": "bun --bun drizzle-kit up",
    "db:push": "bun --bun drizzle-kit push",
    "db:pull": "bun --bun drizzle-kit pull"
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.6.26",
    "@tursodatabase/serverless": "^1.4.0",
    "better-auth": "^1.6.26",
    "drizzle-kit": "1.0.0-rc.4",
    "drizzle-orm": "1.0.0-rc.4"
  },
  "devDependencies": {
    "@libsql/client": "^0.17.4",
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
