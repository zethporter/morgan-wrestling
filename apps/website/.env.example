VITE_APP_TITLE='Morgan Wrestling'

# Read-only Turso credentials. Mint the token with:
#   turso db tokens create <db> --read-only
# This app must never hold a read/write token — see "Read-only" in README.md.
TURSO_CONNECTION_URL=
TURSO_TOKEN=
